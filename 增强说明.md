# AstrBot 电脑控制 + 插件编排增强包

> **作者：yvdi-abc（yudi）** · 日期：2026-08-28 · 插件仓库：[astrbot_plugin_server_ops](https://github.com/yvdi-abc/astrbot_plugin_server_ops)

本包是一套针对 AstrBot 的「电脑/服务器控制 + 插件编排」增强，包含：

1. **`astrbot_plugin_server_ops/`** — 服务器管理工具包插件（主要交付物）
2. 对 AstrBot 运行配置的推荐/已应用改动（见下）

---

## 一、服务器管理工具包（astrbot_plugin_server_ops）

9 个 LLM 工具 + 2 个指令，让机器人可以在对话中直接查看/控制系统状态、管理服务、查看日志、执行安全命令、索引插件能力。详见插件内 `README.md`。

| 工具 | 作用 |
|---|---|
| `server_status` | CPU/内存/负载/运行时间/磁盘 |
| `server_disk_usage` | 磁盘使用详情 |
| `server_process_list` | 进程 TOP 列表 |
| `server_service_ops` | systemd 服务查询/启停 |
| `server_log_view` | 日志查看（限允许路径） |
| `server_exec` | 安全 shell 执行（仅管理员，黑名单拦截） |
| `plugin_index` | 已安装插件能力索引（含停用标注） |
| `astrbot_status` | AstrBot 版本/进程/插件数 |
| `gsuid_status` | gsuid_core 状态 |

**权限**：默认仅管理员（AstrBot `admins_id`）或插件 `allowed_user_ids` 白名单；`server_exec` 与服务启停强制要求管理员。

---

## 二、配套改动（已完成/建议）

### 1. 启用内置 Computer Use
`provider_settings.computer_use_runtime = "local"`
→ 机器人获得内置的 `astrbot_execute_shell`、`astrbot_execute_python`、文件读写/编辑/搜索、浏览器等工具（管理员可用）。

### 2. 省 token：tool_schema_mode
`provider_settings.tool_schema_mode = "skills_like"`
→ 首轮请求只发送工具名+描述（不发送完整参数 schema），模型要调用工具时才二次回传参数。显著降低每轮 tool schema 的 token 开销。代价：调用工具时多一次模型回查（延迟略增）。

### 3. 记忆注入改周期（省 token）
修改 `astrbot_plugin_simple_memory/main.py` 的 `add_mem_prompt`：
- 新增配置 `mem_inject_interval`（默认 **30**）：完整记忆（core/long/medium term + 规则）每 30 轮注入一次，其余轮次只带一句简短提示。
- 记忆开销从 ~2000 token/轮 降至 ~80 token/轮（约省 95%）。
- 设 0 或 1 可恢复原「每轮注入」行为。

### 4. 重新激活被停用的关键 LLM 工具
从 `inactivated_llm_tools`（全局偏好）中恢复：
- `search_memory_by_user_name`、`check_user_roster_id_dict`、`delete_several_memories`（simple_memory 记忆管理）
- 注：enhance_mode / LivelyState 等**插件本身被停用**，其工具保持停用（避免误调用报错）；如需启用请在面板启用对应插件。

### 5. 管理员
`admins_id` 已包含 `3625607718`、`1453957048`、`3829230490` —— 这些账号在私聊中可调用内置 computer-use 与本插件工具。如需增删，编辑 AstrBot 配置的 `admins_id` 或面板「配置-通用配置-管理员」。

---

## 三、测试

```bash
# 用 AstrBot 的 Python 环境运行插件测试
<AstrBot目录>/bin/python astrbot_plugin_server_ops/test_plugin.py
# 期望输出：=== 结果: 42 通过, 0 失败 ===
```

## 四、变更记录

- 2026-08-28：打包 v1.0.0。插件 42/42 测试通过；配置改动已应用到运行中的 AstrBot 并重启验证。
